# lab4(ch6)

## 我实现的功能

* 把上一个lab写的东西改过来
* 实现了 `linkat` 系统调用，在 `DiskINode` 中记录link的数量
* 实现了 `unlinkat` 系统调用，在 link 的数量为0时会回收所有的资源，并把目录项置为0
* 实现了 `fstat` 系统调用，在 `Inode` 中记录对应的ino

## 问答题

1. 在我们的easy-fs中，root inode起着什么作用？如果root inode中的内容损坏了，会发生什么？
起到根目录的作用；如果损坏了，可能会数据丢失、找不到文件、严重的话可能无法挂载文件系统。


